import Form from "../Components/Form";
import Header from "../Components/Header";

const Javascript = () => {
  return (
    <>
      <Header headerTitle="JS rules the world" />
      <Form
        containerTitle="Array.isArray"
        containerContent="return boolean whether value is an array or not"
      />
    </>
  );
};

export default Javascript;
