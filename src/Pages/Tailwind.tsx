import Form from "../Components/Form";
import Header from "../Components/Header";

const Tailwind = () => {
  return (
    <>
      <Header headerTitle="Tailwindcss" />
      <Form
        containerTitle="Tailwindcss"
        containerContent="For TailwindCSS, it has all different command for different system? like Vite, or react-create-app else"
      />
    </>
  );
};

export default Tailwind;
